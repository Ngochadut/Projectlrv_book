$(document).ready(function () {
    $(".btn_cancel").click(function (event) {
        let cancel_id = $(this).attr("data-cancel-id");
        let status = $(this).attr("data-cancel-status");
    });
});